//import { prisma } from "@/lib/db";
//server component rendered on the server and has access to the database
//server component is a react thing not a next.js thing(different from server side rendering).
const Page = () => {
  return <div>Hello World!</div>;
};
export default Page;
